<ResourceDictionary xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">
  <!--
    Style: Soft Flat (Geometrie)
    3px abgerundete Ecken, etwas luftigeres Spacing.
    Farben kommen aus der zuerst gemergten Palette (via DynamicResource).
  -->

  <sys:String xmlns:sys="clr-namespace:System;assembly=mscorlib" x:Key="Theme.StyleMarker">Soft</sys:String>

  <!-- Geometrie — abweichend von Sharp: Ecken 3px, mehr Padding, höhere Zeilen -->
  <CornerRadius x:Key="Theme.Corner">3</CornerRadius>
  <CornerRadius x:Key="Theme.CornerSmall">2</CornerRadius>
  <Thickness    x:Key="Theme.ButtonPadding">14,6</Thickness>
  <Thickness    x:Key="Theme.TabItemPadding">18,8</Thickness>
  <Thickness    x:Key="Theme.CellPadding">10,7</Thickness>
  <System:Double xmlns:System="clr-namespace:System;assembly=mscorlib" x:Key="Theme.RowHeight">28</System:Double>
  <System:Double xmlns:System="clr-namespace:System;assembly=mscorlib" x:Key="Theme.FontSize">12</System:Double>

  <!-- Button -->
  <Style TargetType="Button">
    <Setter Property="Background" Value="{DynamicResource Theme.Background}"/>
    <Setter Property="Foreground" Value="{DynamicResource Theme.TextPrimary}"/>
    <Setter Property="BorderBrush" Value="{DynamicResource Theme.Border}"/>
    <Setter Property="BorderThickness" Value="1"/>
    <Setter Property="Padding" Value="{StaticResource Theme.ButtonPadding}"/>
    <Setter Property="Margin" Value="0,0,6,0"/>
    <Setter Property="Cursor" Value="Hand"/>
    <Setter Property="FontSize" Value="{StaticResource Theme.FontSize}"/>
    <Setter Property="SnapsToDevicePixels" Value="True"/>
    <Setter Property="Template">
      <Setter.Value>
        <ControlTemplate TargetType="Button">
          <Border x:Name="Bd" Background="{TemplateBinding Background}"
                  BorderBrush="{TemplateBinding BorderBrush}"
                  BorderThickness="{TemplateBinding BorderThickness}"
                  CornerRadius="{StaticResource Theme.Corner}"
                  SnapsToDevicePixels="True">
            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"
                              Margin="{TemplateBinding Padding}"
                              RecognizesAccessKey="True"/>
          </Border>
          <ControlTemplate.Triggers>
            <Trigger Property="IsMouseOver" Value="True">
              <Setter TargetName="Bd" Property="Background" Value="{DynamicResource Theme.AccentLight}"/>
              <Setter TargetName="Bd" Property="BorderBrush" Value="{DynamicResource Theme.Accent}"/>
            </Trigger>
            <Trigger Property="IsPressed" Value="True">
              <Setter TargetName="Bd" Property="Background" Value="{DynamicResource Theme.Accent}"/>
              <Setter Property="Foreground" Value="White"/>
            </Trigger>
            <Trigger Property="IsEnabled" Value="False">
              <Setter Property="Foreground" Value="{DynamicResource Theme.TextSecondary}"/>
              <Setter TargetName="Bd" Property="Background" Value="{DynamicResource Theme.SoftBg}"/>
            </Trigger>
          </ControlTemplate.Triggers>
        </ControlTemplate>
      </Setter.Value>
    </Setter>
  </Style>

  <!-- TextBox -->
  <Style TargetType="TextBox">
    <Setter Property="Background" Value="{DynamicResource Theme.Background}"/>
    <Setter Property="Foreground" Value="{DynamicResource Theme.TextPrimary}"/>
    <Setter Property="BorderBrush" Value="{DynamicResource Theme.Border}"/>
    <Setter Property="BorderThickness" Value="1"/>
    <Setter Property="Padding" Value="6,4"/>
    <Setter Property="FontSize" Value="{StaticResource Theme.FontSize}"/>
    <Setter Property="VerticalContentAlignment" Value="Center"/>
  </Style>

  <!-- TabItem — leicht runde obere Ecken -->
  <Style TargetType="TabItem">
    <Setter Property="Padding" Value="{StaticResource Theme.TabItemPadding}"/>
    <Setter Property="FontSize" Value="{StaticResource Theme.FontSize}"/>
    <Setter Property="Background" Value="Transparent"/>
    <Setter Property="Foreground" Value="{DynamicResource Theme.TextPrimary}"/>
    <Setter Property="Template">
      <Setter.Value>
        <ControlTemplate TargetType="TabItem">
          <Border x:Name="Bd" Background="Transparent" CornerRadius="3,3,0,0" BorderThickness="0,0,0,2" BorderBrush="Transparent">
            <ContentPresenter ContentSource="Header" HorizontalAlignment="Center" VerticalAlignment="Center"
                              Margin="{TemplateBinding Padding}"/>
          </Border>
          <ControlTemplate.Triggers>
            <Trigger Property="IsSelected" Value="True">
              <Setter TargetName="Bd" Property="BorderBrush" Value="{DynamicResource Theme.Accent}"/>
              <Setter Property="FontWeight" Value="SemiBold"/>
            </Trigger>
            <Trigger Property="IsMouseOver" Value="True">
              <Setter TargetName="Bd" Property="Background" Value="{DynamicResource Theme.SoftBg}"/>
            </Trigger>
          </ControlTemplate.Triggers>
        </ControlTemplate>
      </Setter.Value>
    </Setter>
  </Style>

  <!-- TabControl -->
  <Style TargetType="TabControl">
    <Setter Property="Background" Value="{DynamicResource Theme.Background}"/>
    <Setter Property="BorderBrush" Value="{DynamicResource Theme.Border}"/>
    <Setter Property="BorderThickness" Value="0"/>
    <Setter Property="Padding" Value="0"/>
  </Style>

  <!-- DataGrid: Scrollbars permanent sichtbar. -->
  <Style TargetType="DataGrid">
    <Setter Property="Background" Value="{DynamicResource Theme.Background}"/>
    <Setter Property="Foreground" Value="{DynamicResource Theme.TextPrimary}"/>
    <Setter Property="BorderBrush" Value="{DynamicResource Theme.Border}"/>
    <Setter Property="BorderThickness" Value="1"/>
    <Setter Property="GridLinesVisibility" Value="Horizontal"/>
    <Setter Property="HorizontalGridLinesBrush" Value="{DynamicResource Theme.GridLine}"/>
    <Setter Property="RowBackground" Value="{DynamicResource Theme.Background}"/>
    <Setter Property="AlternatingRowBackground" Value="{DynamicResource Theme.SoftBg}"/>
    <Setter Property="HeadersVisibility" Value="Column"/>
    <Setter Property="RowHeight" Value="{StaticResource Theme.RowHeight}"/>
    <Setter Property="FontSize" Value="{StaticResource Theme.FontSize}"/>
    <Setter Property="AutoGenerateColumns" Value="False"/>
    <Setter Property="CanUserAddRows" Value="False"/>
    <Setter Property="CanUserDeleteRows" Value="False"/>
    <Setter Property="IsReadOnly" Value="True"/>
    <Setter Property="ScrollViewer.VerticalScrollBarVisibility"   Value="Visible"/>
    <Setter Property="ScrollViewer.HorizontalScrollBarVisibility" Value="Auto"/>
  </Style>
  <Style TargetType="DataGridColumnHeader">
    <Setter Property="Background" Value="{DynamicResource Theme.HeaderBg}"/>
    <Setter Property="Foreground" Value="{DynamicResource Theme.TextPrimary}"/>
    <Setter Property="FontWeight" Value="SemiBold"/>
    <Setter Property="Padding" Value="{StaticResource Theme.CellPadding}"/>
    <Setter Property="BorderBrush" Value="{DynamicResource Theme.Border}"/>
    <Setter Property="BorderThickness" Value="0,0,1,1"/>
    <Setter Property="HorizontalContentAlignment" Value="Left"/>
  </Style>
  <Style TargetType="DataGridCell">
    <Setter Property="BorderThickness" Value="0"/>
    <Setter Property="Padding" Value="10,4"/>
    <Setter Property="Template">
      <Setter.Value>
        <ControlTemplate TargetType="DataGridCell">
          <Border Background="{TemplateBinding Background}" BorderBrush="{TemplateBinding BorderBrush}"
                  BorderThickness="{TemplateBinding BorderThickness}" Padding="{TemplateBinding Padding}">
            <ContentPresenter VerticalAlignment="Center"/>
          </Border>
        </ControlTemplate>
      </Setter.Value>
    </Setter>
    <Style.Triggers>
      <Trigger Property="IsSelected" Value="True">
        <Setter Property="Background" Value="{DynamicResource Theme.AccentLight}"/>
        <Setter Property="Foreground" Value="{DynamicResource Theme.TextPrimary}"/>
      </Trigger>
    </Style.Triggers>
  </Style>

  <!-- Menu -->
  <Style TargetType="Menu">
    <Setter Property="Background" Value="{DynamicResource Theme.Background}"/>
    <Setter Property="Foreground" Value="{DynamicResource Theme.TextPrimary}"/>
    <Setter Property="BorderBrush" Value="{DynamicResource Theme.Border}"/>
    <Setter Property="BorderThickness" Value="0,0,0,1"/>
    <Setter Property="Padding" Value="6"/>
  </Style>

  <!-- StatusBar -->
  <Style TargetType="StatusBar">
    <Setter Property="Background" Value="{DynamicResource Theme.Background}"/>
    <Setter Property="Foreground" Value="{DynamicResource Theme.TextPrimary}"/>
    <Setter Property="BorderBrush" Value="{DynamicResource Theme.Border}"/>
    <Setter Property="BorderThickness" Value="0,1,0,0"/>
    <Setter Property="Padding" Value="10,6"/>
    <Setter Property="FontSize" Value="{StaticResource Theme.FontSize}"/>
  </Style>

  <!-- Label-Variante für sekundären Text -->
  <Style x:Key="Theme.Secondary" TargetType="TextBlock">
    <Setter Property="Foreground" Value="{DynamicResource Theme.TextSecondary}"/>
  </Style>
</ResourceDictionary>
