# Session Handoff — 2026-07-06 00:21 (Session 1)

Erste Handoff-Datei des Projekts (Muster wie luDBxP). Persistenter Kontext liegt
zusätzlich in der lokalen Projekt-Memory `oupilot.md`.

## Was wir gemacht haben
Aus einem lokalen, nicht versionierten Ordner ein **GitHub-Repo** gemacht
(`github.com/monoeagle/lucent-job-OUPilot`, PUBLIC) und in einer Session die
komplette offene README-Restliste abgearbeitet: **Theme-System**, **Baum-Filter**,
**Mitglieder entfernen**, **konfigurierbare Feld-Map** — je verifiziert (Headless-
Smoke-Tests unter PS 5.1), dokumentiert und einzeln committet/gepusht. Zum
Abschluss **Testclient-Checkliste** geschrieben und **GitHub-Release v1.4.0** mit
App-Zip erstellt.

## Erledigt
- [x] **Git-Init + erster Push** (`main`), `.gitignore` (Logs, `data/mapping.json`,
      `fieldmap.json`, `samples/__*.ps1`). Repo bewusst **PUBLIC** (vom Nutzer bestätigt).
- [x] **v1.1.0 Theme-System** — aus CodeSigningCommander portiert: `ui/theme-loader.psm1`
      (`Initialize-Theme`/`Switch-Theme`) + `ui/themes/{sharp,soft}.xaml` +
      `ui/themes/palettes/*.xaml` (12 Paletten). Menü *Ansicht* → Farbschema/Stil live
      umschaltbar, Settings-Keys `UiStyle`/`UiPalette`. Fenster nutzen
      `DynamicResource Theme.*` statt hartkodierter Farben.
- [x] **v1.2.0 Baum-Filter** — Suchfeld über der TreeView; `_Oup-AddTreeNodes` ersetzt
      durch `_Oup-BuildFilteredItem`/`_Oup-RenderTree` (Vorfahren von Treffern bleiben
      sichtbar, matcht ein OU-Name selbst → ganzer Teilbaum; OU-Knoten bei Filter offen).
- [x] **v1.3.0 Mitglieder entfernen** — `Remove-OupGroupMembers` (ad-writer, Modul/ADSI/
      Mock, Status `Removed`/`NotMember`/…) + `Remove-OupImportEntries` (store, $-/case-
      tolerant). Button „Ausgewählte entfernen", Grid-Mehrfachauswahl, WhatIf + Bestätigung;
      purged aus Store nur, was danach kein Mitglied mehr ist; Baumzähler korrigiert.
- [x] **v1.4.0 Feld-Map** — optionale `fieldmap.json` (`Get-/Import-/Set-OupFieldMap` in
      import-engine, Settings-Key `FieldMapPath`); eigene Feldnamen custom-first + dedupe,
      beim Start angewandt, Statuszeile/Log melden Zahl. Vorlage `samples/fieldmap.example.json`.
- [x] **docs/Testclient-Checkliste.md** (aus README verlinkt) + **GitHub-Release v1.4.0**
      (Tag `v1.4.0`, Latest, Asset `OUPilot-1.4.0.zip` via `git archive`). README mit
      Release-/Download-Links.

## In Arbeit / Nächste Schritte
- [ ] **⚠️ Echtes AD am Domänen-Testclient gegenprüfen** — bislang ist nur der **Mock**-Pfad
      verifiziert (Dev-Client ist bewusst AD-abgekoppelt). Add-/Remove-Pfade (Modul/ADSI)
      Schritt für Schritt nach **`docs/Testclient-Checkliste.md`** validieren (immer erst
      WhatIf, dann echt; nur Test-OUs/-Gruppen/-Rechner). Hier prinzipbedingt nicht möglich.
- [ ] Danach: gefundene Findings einarbeiten oder neues Feature. Die ursprüngliche
      README-Restliste ist **abgearbeitet**.

## Entscheidungen dieser Session
- **Theme 1:1 aus CSC** übernommen (Palette-XAML = nur Brushes, Style-XAML = Geometrie +
  Control-Styles via DynamicResource). Bewusste Einschränkung wie CSC: Palettenwechsel greifen
  live, **Stilwechsel** (Geometrie via StaticResource) voll erst beim Neustart.
- **Feld-Map erweitert** (custom-first + dedupe), ersetzt **nicht** — bestehende Formate
  bleiben unberührt; `fieldmap.json` ist gitignored (site-spezifisch), Vorlage in `samples/`.
- **Entfernen destruktiv** → Warn-Dialog + WhatIf; Store-Purge nur für `Removed`/`NotMember`/
  `Simuliert`.
- **Release-Asset via `git archive`** (nur getrackte Dateien, kein Runtime/Logs) statt Build-Skript.

## Git-Status
- Branch `main` = `origin/main`, working tree clean. HEAD nach diesem Handoff-Commit
  (vorher `fa0fe0a`). Tag **`v1.4.0`** existiert (Release Latest).
- Nicht im Repo (bewusst): `data/mapping.json` (Runtime-Store), `Logs/*`, `fieldmap.json`
  (site-spezifisch), `samples/__*.ps1` (Headless-Tests).

## Verifikation
- Alle Features per Headless-Smoke-Test (PS 5.1) grün: Theme laden/switch (12 Paletten/2 Stile),
  Baum-Filter (voll 218 → 'Berli' 83/82 Treffer → Reset), Remove (Store $-tolerant + Mock-AD),
  Feld-Map (exotisches Format vor/nach Map + idempotent), Startup mit `fieldmap.json` → Log-Note.
- `tools\Ensure-Utf8Bom.ps1` je 12/12 OK, 0 Parserfehler. UI-Render via `-SelfTestMs`.
- **Nicht** verifiziert: echtes AD-Lesen/-Schreiben (kein Domänenclient hier).

---
## Resume-Prompt (kopieren für neue Session)

Weitermachen mit OUPilot (github.com/monoeagle/lucent-job-OUPilot, PUBLIC, `main`).
Stand: **v1.4.0 released**, working tree clean; alle README-Features fertig
(Theme/Filter/Entfernen/Feld-Map). WPF auf **Windows PowerShell 5.1** (Desktop, STA);
Start `.\run.ps1`. Dev-Client ist **AD-abgekoppelt → nur Mock testbar**.
ZUERST: `git fetch` + Divergenz prüfen. **Nächster Schritt:** echtes AD am
Domänen-Testclient gegenprüfen per `docs/Testclient-Checkliste.md` (erst WhatIf,
dann echt) — geht nur auf einem echten Domänenclient, hier nicht.
Workflow-Pflicht: nach jedem `.ps1/.psm1`-Edit `tools\Ensure-Utf8Bom.ps1` (Write/Edit
schreibt ohne BOM → PS 5.1 zerlegt Umlaute). Headless-Tests als `samples\__*.ps1`
(gitignored), PS 5.1 `-STA`, **ASCII only** (keine Em-Dashes!), danach löschen; UI via
`Show-OupMainWindow -SelfTestMs <ms>`. Bei neuem Release: About-Version +
CHANGELOG-Abschnitt → `git tag -a vX.Y.Z` → `git archive --format=zip
--prefix=OUPilot-X.Y.Z/ -o <zip> vX.Y.Z` → push main+Tag → `gh release create
vX.Y.Z --title ... --notes-file ... --latest <zip>`.
Handoff-Datei: docs/handoffs/2026-07-06-0021.md
