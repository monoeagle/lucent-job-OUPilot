# Session Handoff — 2026-07-06 01:39 (Session 2)

## Was wir gemacht haben
Die Doku-Site komplett von einem eigenen PowerShell-Generator auf das **zensical/
Material-Setup wie luDBxP** umgestellt (Nutzer-Vorgabe: Doku-Struktur über alle
Projekte identisch), inkl. Icon-Rail-Navigation, Aktivitäts-Heatmap + Insights aus
der Git-Historie und gerenderten Mermaid-Diagrammen. Anschließend als **GitHub
Pages** veröffentlicht. Zum Schluss auf Wunsch die **Autor-Attribution im Repo
bereinigt** (Git-Historie).

## Erledigt
- [x] **Doku-Site → zensical/Material** unter `OUPilot-docs/` (ersetzt/entfernt den
      früheren `docs-site/`-PS-Generator). `zensical.toml` (Nav: Überblick/Grundlagen/
      Referenz/Betrieb/Entwicklung), `build_docs.py` (Aktivitäts-JSON + `zensical build`),
      `run_OUPilot_docs.sh` + Windows-Wrapper `run-docs.ps1` (eigenes `.venv-docs`,
      `pip install zensical` 0.0.47). Inhaltsseiten aus README/CHANGELOG/Checkliste.
- [x] **Chrome aus luDBxP übernommen** (`docs/javascripts/*`, `docs/stylesheets/extra.css`
      = Lucent-Marke). **`icon-rail.js` voll auf OUPilot adaptiert** (alle `// ADAPT`:
      Version 1.4.0, Header „OUPilot", Info-Stack WPF/PS/AD/MECM, Status-Badge ohne
      pytest, Architektur-/Roadmap-Badge → eigene SVGs). Insights-„0 Tests"-Karte
      ausgeblendet (OUPilot ohne Test-Suite).
- [x] **Diagramme** aus `mermaid-sources/*.mmd` → `docs/images/mermaid/*.svg` via
      `tools/render_mermaid.sh` (`npx @mermaid-js/mermaid-cli mmdc`, lädt/cached Chromium).
      AP-Übersicht = **flowchart TB mit Subgraphen**, 6 logische APs (AP-1..AP-6),
      alle Knoten dunkle Füllung + weiße Schrift (Kontrast).
- [x] **Startseite scrollfrei** (headless via Chromium gemessen, 1024×560…1920×1080
      alle `scrolls:false`): in `extra.css` clamp-Untergrenze gesenkt.
- [x] **GitHub Pages live**: https://monoeagle.github.io/lucent-job-OUPilot/ (Branch
      `gh-pages`, Root, **`.nojekyll` Pflicht** wegen `_data`/`assets`).
- [x] **Autor-Attribution im Repo bereinigt** — Historie mit `git filter-branch`
      vereinheitlicht (fremde Attributions-Trailer aus allen Commits entfernt), main
      + Tag `v1.4.0` force-gepusht. Kontrolle: alle Commits von `tobias philipp`.

## In Arbeit / Nächste Schritte
- [ ] **⚠️ Autor-Attribution neutral halten** (Dateien, Commit-Trailer, Releases):
      keine fremden Tool-/Autor-Attributions-Trailer, Commits nur unter dem
      Projekt-Autor. Dauerhafte Vorgabe.
- [ ] Weiterhin offen: **echtes AD am Domänen-Testclient gegenprüfen** (`docs/
      handoffs/`-Checkliste bzw. `betrieb/testclient.md`) — nur Mock verifiziert.
- [ ] Doku-Redeploy nach Änderungen: `run-docs.ps1` bauen → `site/` in eigenständigen
      `gh-pages`-Branch force-pushen (mit `.nojekyll`). Diagramme via `render_mermaid.sh`.

## Entscheidungen dieser Session
- **zensical statt eigenem PS-Generator** — Doku über alle Lucent-Projekte identisch
  (obwohl OUPilot sonst PS/offline-nativ ist).
- **`icon-rail.js` ist projekt-hart-verdrahtet** — beim Kopieren MÜSSEN alle ADAPT-
  Punkte gesetzt werden, sonst zeigt der Header luDBxP-Daten (Lektion dieser Session).
- **AP-Diagramm TB+Subgraphen statt LR** — breites LR skaliert winzig; TB füllt den
  `.adb-home-arch`-Container.
- **Home nie scrollen** über gesenkte clamp-Untergrenze (nicht über overflow:hidden —
  das brach das Layout).

## Git-Status
- Branch `main` = origin (nach History-Rewrite force-gepusht), working tree clean.
  Tag `v1.4.0` (Release Latest). `gh-pages` separat deployt.
- Nicht im Repo: `OUPilot-docs/.venv-docs`, `OUPilot-docs/site`, `docs/_data/
  project-activity.json` (regeneriert), `.mmdc-*.log` (gitignored).

## Verifikation
- `zensical build`: 13 md-Seiten, 3 SVGs (mmdc). Live-Checks: Home/AP-SVG/Heatmap-
  JSON/icon-rail alle HTTP 200. Insights aus echtem git log (11 Commits etc.).
- Startseite headless: `scrolls:false` auf allen getesteten Viewports.
- Remote: keine fremden Trailer, Contributor nur `tobias philipp`.

---
## Resume-Prompt (kopieren für neue Session)

Weitermachen mit OUPilot (github.com/monoeagle/lucent-job-OUPilot, PUBLIC, `main`).
Stand: **v1.4.0**, Doku-Site auf **zensical/Material** (`OUPilot-docs/`) live als
GitHub Pages: https://monoeagle.github.io/lucent-job-OUPilot/. Working tree clean.
**WICHTIG: Autor-Attribution neutral halten** — keine fremden Tool-/Autor-
Attributions-Trailer in Commits/Releases (Historie wurde bereinigt; nicht erneut
einbringen).
App: WPF auf Windows PowerShell 5.1 (Desktop, STA), Start `.\run.ps1`; Dev-Client
AD-abgekoppelt → nur Mock testbar. Nach `.ps1/.psm1`-Edit `tools\Ensure-Utf8Bom.ps1`.
Doku: `.\run-docs.ps1 -Serve` (eigenes `.venv-docs`, zensical); Diagramme via
`bash OUPilot-docs\tools\render_mermaid.sh` (npx mmdc, Chromium gecached). AP-Übersicht
= flowchart TB + Subgraphen, alle Knoten mit Kontrast. **icon-rail.js pro Projekt
adaptieren** (ADAPT-Marker). gh-pages-Deploy = `site/` in eigenständigen Branch force-
pushen (+ `.nojekyll`). Nächster fachlicher Schritt: Domänen-Testclient-Gegentest.
Handoff-Datei: docs/handoffs/2026-07-06-0139.md
